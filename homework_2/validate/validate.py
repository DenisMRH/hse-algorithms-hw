def isability (pushed: str,poped : str) -> bool:
    pushed = ((pushed).strip()).split()
    poped = ((poped).strip()).split()
    print(pushed,poped)



pushed = input("pushed: ")
poped = input("poped: ")
isability(pushed, poped)